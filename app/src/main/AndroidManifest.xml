<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kei.mobilehero" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.GamesActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.NewGameActivity"
            android:label="@string/title_activity_new_game" >
        </activity>
        <activity
            android:name=".activities.DicesActivity"
            android:label="@string/title_activity_dices" >
        </activity>
        <activity
            android:name=".activities.RoundsActivity"
            android:label="@string/title_activity_rounds" >
        </activity>
        <activity
            android:name=".activities.NewRoundActivity"
            android:label="@string/title_activity_new_round" >
        </activity>
        <activity
            android:name=".activities.CharactersActivity"
            android:label="@string/title_activity_characters" >
        </activity>
    </application>

</manifest>
